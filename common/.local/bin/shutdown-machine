#!/bin/sh -i

self_basename="$(basename "$0")"

if [ "$self_basename" = "shutdown-machine" ]; then
  echo "Please execute a linked binary instead!"
  exit 1
fi

machine_name="$(echo "$self_basename" | sed 's/^shutdown-\(.*\)/\1/')"

ssh -t "$machine_name" "sudo shutdown now"
