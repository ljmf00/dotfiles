#!/usr/bin/env bash

# shellcheck disable=SC2068
youtube-dl \
    -f bestaudio \
    --extract-audio \
    --audio-format mp3 \
    --audio-quality 0 \
    --embed-thumbnail \
    --add-metadata \
    -i \
    -o "%(uploader)s - %(title)s.%(ext)s" \
    $@
